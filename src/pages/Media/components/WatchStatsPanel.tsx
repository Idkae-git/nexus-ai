import type { WatchStats } from '../../../features/watch-tracking/watch.types.ts'

function duration(seconds: number) { const hours = Math.floor(seconds / 3600); const minutes = Math.floor(seconds % 3600 / 60); return `${hours} h ${minutes} min` }
export function WatchStatsPanel({ stats }: { stats: WatchStats | null }) { return <section className="watch-stats"><header><div><span>HONEST STATISTICS</span><h2>Ce que NEXUS sait vraiment</h2></div><small>Durées inconnues exclues</small></header><div><article><span>Progression exacte</span><strong>{duration(stats?.exactDurationSeconds ?? 0)}</strong><small>Lectures locales mesurées</small></article><article><span>Activité sans durée</span><strong>{stats?.activityWithoutDuration ?? 0}</strong><small>Imports ou lancements sans position</small></article><article><span>Films identifiés</span><strong>{stats?.movies ?? 0}</strong><small>Sans durée inventée</small></article><article><span>Épisodes enregistrés</span><strong>{(stats?.episodes ?? 0) + (stats?.animeEntries ?? 0)}</strong><small>Séries et animés</small></article></div></section> }
